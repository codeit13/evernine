# Copy to .env and fill in. The service reads OPENAI_API_KEY at call time only;
# it is never stored or logged. Without it, scoring still works and the
# explanation falls back to the deterministic template.
OPENAI_API_KEY=sk-...

# Optional overrides:
# DATA_PATH=business_signals.json
# OPENAI_MODEL=gpt-4o-mini
