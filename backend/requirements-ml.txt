# OPTIONAL: local transformer sentiment for the reviews signal.
# Install with:  pip install -r requirements-ml.txt
# If these are absent (or the model can't be downloaded), the reviews signal
# transparently falls back to a built-in lexicon sentiment scorer -- the service
# still runs and stays deterministic. The first run with these installed will
# download ~250MB for distilbert-base-uncased-finetuned-sst-2-english.
torch>=2.1
transformers>=4.38
